/// @author    Johannes de Fine Licht (definelicht@inf.ethz.ch)
/// @date      June 2018 
/// @copyright This software is copyrighted under the BSD 3-Clause License. 

#pragma once

#include <cstddef>
#include "hlslib/Operators.h"
#include <cstdint>
#include "hls_half.h"

using Data_t = ${MM_DATA_TYPE}; 
constexpr int kMemoryWidthBytes = ${MM_MEMORY_BUS_WIDTH};
constexpr int kSizeN = ${MM_SIZE_N};
constexpr int kSizeK = ${MM_SIZE_K};
constexpr int kSizeM = ${MM_SIZE_M};
constexpr int kOuterTileSize = ${MM_OUTER_MEMORY_TILE_SIZE};
constexpr int kInnerTileSizeN = ${MM_OUTER_COMPUTE_TILE_SIZE_N};
constexpr int kInnerTileSizeM = ${MM_OUTER_COMPUTE_TILE_SIZE_M};
constexpr int kComputeTileSizeN = ${MM_INNER_COMPUTE_TILE_SIZE_N};
constexpr int kComputeTileSizeM = ${MM_INNER_COMPUTE_TILE_SIZE_M};
constexpr int kTransposeWidthBytes = ${MM_TRANSPOSE_WIDTH};
constexpr float kFrequency = ${MM_TARGET_CLOCK};
constexpr auto kGoldenDir = "${MM_GOLDEN_DIR}"; 

using OperatorMap = hlslib::op::${MM_MAP_OP}<Data_t>;
using OperatorReduce = hlslib::op::${MM_REDUCE_OP}<Data_t>;
